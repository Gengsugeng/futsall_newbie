<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.g.futsalapp">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission	android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LoginActivity" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".FutsalApp"
            android:label="@string/title_activity_futsal_app"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".CekLapanganActivity"
            android:label="@string/title_activity_cek_lapangan"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Transaksi" />
        <activity android:name=".QRActivity"></activity>
    </application>

</manifest>